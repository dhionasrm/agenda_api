[phases.setup]
nixPkgs = ["bun"]

[phases.install]
cmds = ["bun install"]

[phases.build]
cmds = ["bunx prisma generate"]

[start]
cmd = "bunx prisma db push --accept-data-loss && bun run src/server.ts"